package shared;


/*
 * Interface for methods of the Node class that can be invoked by
 * angiogenic rule sets.
 */

public interface NodeInterface {

    public static enum ShapeType {SPHERE, CAPSULE};

    /*
    public void remove(EnvironmentInterface e);

    public void create(NodeData nd,
		       EnvironmentInterface e);

    public void translate(double deltaX, double deltaY, double deltaZ,
			  EnvironmentInterface e);
    */
}
